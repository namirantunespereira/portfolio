const gallery=document.getElementById('gallery');const layers=Array.from(document.querySelectorAll('.layer'));function moveLayers(mx,my){const rect=gallery.getBoundingClientRect();const cx=rect.left+rect.width/2;const cy=rect.top+rect.height/2;const dx=(mx-cx)/rect.width;const dy=(my-cy)/rect.height;layers.forEach(l=>{const d=parseFloat(l.dataset.depth);l.style.transform=`translate(${dx*d*80}px, ${dy*d*60}px)`;});}window.addEventListener('mousemove',(e)=>moveLayers(e.clientX,e.clientY));window.addEventListener('scroll',()=>{const y=window.scrollY+window.innerHeight/2;moveLayers(window.innerWidth/2,y);});